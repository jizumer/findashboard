# findashboard
NodeJS + MongoDB Financial &amp; Stocks Personal Dashboard

This apps expects a mongoDB to be running and accessible.

To run the app, first set up the file config.json, with parameters such as database connection.

Once the config file is ready, type: "npm start". It will run nodemon, so code changes will be automatically detected.